#include <stdio.h>
/*
int main() {
	int a, b;
	int * address, * poval;
	printf("정수 2개를 입력하시오(구분자는 콤마 사용). :");
	scanf_s("%d, %d", &a, &b);
	address = &a;
	poval = &b;
	printf("입력한 정수 2개는 %d, %d\n", a, b);
	printf("3의 주소값 : %d\n", address);
	printf("5의 주소값 : %d", poval);
}*/