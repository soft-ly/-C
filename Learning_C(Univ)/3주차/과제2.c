#include <stdio.h>
/*
int main() {
	int a, b, c, val;
	printf("키보드로 정수값 3개를 입력하시오(구분자는 콤마 사용). : ");
	scanf_s("%d, %d, %d", &a, &b, &c);
	val = (b <= a) ? ((c <= a) ? a : c) : ((c <= b) ? b : c);
	printf("키보드로 입력한 정수값 3개는 %d, %d, %d\n", a, b, c);
	printf("그중에서 최댓값은 %d", val);
	return 0;
}*/